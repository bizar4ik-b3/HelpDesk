@model Department
@using HelpDeskTrain.Models
@{
    ViewBag.Title = "Кафедри/Відділи";
}
<h2>Кафедри/Відділи</h2>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Нова Кафедра/Відділ</legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <p><input type="submit" value="Зберегти" /></p>
    </fieldset>
}
<table>
@foreach (Department item in ViewBag.Departments)
{
    <tr>
        <td>@item.Id</td>
        <td>@item.Name</td>
        <td>@Html.ActionLink("Видалити", "DeleteDepartment", new { id=item.Id })</td>
    </tr>
}
</table>
